<!-- general page container -->
<div class="container">
    <div class="container-content">
<!-- loop through api data -->
        {{#each items}}
            <p>{{this}}</p>
        {{/each}}
    </div>
</div>