{{!-- <!-- general page container -->
<div class="container">
    <div class="container-content">
<!-- loop through api data -->
        {{#each results as |result|}}
            
            <h2> {{result.array}}</h2>
            
        {{/each}}
    </div>
</div>
<div id="container-butHome">
          <button onclick="homePage()" id="butHome">- - Home - -</button>
    </div>


<script>
    function homePage() {
      window.location.href = '/';
    }
</script> --}}

<!-- general page container -->
<div class="container">
    <div class="container-content">
<!-- each card displays 1 item from GPT response -->
        <div class="results-card">
            <div class="results-content">
                <!-- content in this span-->
                <span class="drac-text">
                    {{#each results as |result|}}
            
            <h2> {{result.array}}</h2>
            
        {{/each}}
                </span>
            </div>
            <button id="buttSave"> Save </button>
            {{!-- <button id="buttSave" onclick="homePage()" id="butHome"> Home </button> --}}
        </div>
    </div>
</div>
<script>
    function homePage() {
      window.location.href = '/';
    }
</script>